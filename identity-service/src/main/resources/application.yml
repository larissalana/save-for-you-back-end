spring:
  application:
    name: identity-service
  profiles:
    active: default
  cloud:
    config:
      name: identity-service
  config:
    import: optional:configserver:http://3.85.123.230:8888
